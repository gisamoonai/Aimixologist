
  # AI Mixologist

  This is a code bundle for AI Mixologist. The original project is available at https://www.figma.com/design/gJN4ZoL6zG0AOhPWKtOr2E/AI-Mixologist.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  